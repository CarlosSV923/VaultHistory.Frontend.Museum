# Public service origins; values may be omitted until their integration story is implemented.
NEXT_PUBLIC_USER_API_URL=http://localhost:3001

# Server-only History integration. Never prefix either variable with NEXT_PUBLIC_.
HISTORY_API_URL=http://localhost:3000
HISTORY_FRONTEND_TOKEN=replace-with-a-local-development-token
